{
  "id": "fZ6oxWLAC7XR0Rb6",
  "name": "RetrievalSlackDispatch",
  "nodes": [
    {
      "parameters": {
        "workflowInputs": {
          "values": [
            {
              "name": "run_id"
            },
            {
              "name": "case_id"
            },
            {
              "name": "channel_id"
            },
            {
              "name": "bot_id"
            }
          ]
        }
      },
      "id": "b6399a96-bc42-47a7-b50d-3eff85343b9f",
      "name": "Case Input",
      "type": "n8n-nodes-base.executeWorkflowTrigger",
      "typeVersion": 1.2,
      "position": [
        -640,
        304
      ]
    },
    {
      "parameters": {
        "operation": "getAll",
        "tableId": "retrieval_cases",
        "returnAll": true,
        "matchType": "allFilters",
        "filters": {
          "conditions": [
            {
              "keyName": "id",
              "condition": "eq",
              "keyValue": "={{ $('Case Input').item.json.case_id }}"
            }
          ]
        }
      },
      "id": "6c59a403-89b3-4b76-a8bb-08c4773fdc72",
      "name": "Load Case",
      "type": "n8n-nodes-base.supabase",
      "typeVersion": 1,
      "position": [
        -416,
        304
      ],
      "credentials": {
        "supabaseApi": {
          "id": "gjrMKYK5nCbL3TLf",
          "name": "Supabase Tests"
        }
      }
    },
    {
      "parameters": {
        "jsCode": "const c = $input.first().json;\nlet ctx = c.context_messages;\nif (typeof ctx === 'string') { try { ctx = JSON.parse(ctx); } catch (e) { ctx = []; } }\nif (!Array.isArray(ctx)) ctx = [];\nconst turns = ctx.map(m => ({ text: String(m), is_first: false, is_final: false }));\nturns.push({ text: c.prompt, is_first: false, is_final: true });\nif (turns.length) turns[0].is_first = true;\nreturn turns.map(t => ({ json: t }));"
      },
      "id": "a435ac2b-87a0-499d-89a3-dbaa79eeaf3b",
      "name": "Build Turns",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -192,
        304
      ]
    },
    {
      "parameters": {
        "resource": "channel",
        "operation": "replies",
        "channelId": {
          "__rl": true,
          "mode": "id",
          "value": "={{ $('Case Input').item.json.channel_id }}"
        },
        "ts": "={{ $('Final Ts').first().json.thread_ts }}",
        "limit": 100,
        "filters": {}
      },
      "id": "b31ecfc5-fee8-4172-a413-34eaf2e8c639",
      "name": "Fetch Thread",
      "type": "n8n-nodes-base.slack",
      "typeVersion": 2.3,
      "position": [
        32,
        608
      ],
      "webhookId": "8c078900-0d96-4238-8aa5-c02a02ede278",
      "credentials": {
        "slackApi": {
          "id": "xyfYErKBBCTrIiMl",
          "name": "Robs Slack"
        }
      }
    },
    {
      "parameters": {
        "filters": {
          "conditions": [
            {
              "condition": "eq",
              "keyName": "session_id",
              "keyValue": "={{ $('Final Ts').first().json.thread_ts }}"
            }
          ]
        },
        "matchType": "allFilters",
        "operation": "getAll",
        "returnAll": true,
        "tableId": "agent_trace"
      },
      "id": "61edcc84-2801-4e5f-87ed-2c6ca0930b20",
      "name": "Fetch Trace",
      "type": "n8n-nodes-base.supabase",
      "typeVersion": 1,
      "position": [
        256,
        608
      ],
      "alwaysOutputData": true,
      "credentials": {
        "supabaseApi": {
          "id": "gBIPPKjo4dshypxE",
          "name": "Supabase API"
        }
      }
    },
    {
      "parameters": {
        "jsCode": "const c = $('Load Case').item.json;\nconst runId = $('Case Input').item.json.run_id;\nconst caseId = $('Case Input').item.json.case_id;\nconst botId = $('Case Input').item.json.bot_id;\nconst threadTs = $('Final Ts').first().json.thread_ts;\n\nconst finalReactions = ($('Final Reactions').first().json.message && $('Final Reactions').first().json.message.reactions) || [];\nconst turnErrored = finalReactions.some(r => r.name === 'x');\n\nconst replies = $('Fetch Thread').all().map(i => i.json);\nconst botMsgs = replies.filter(m => m.user === botId || m.bot_id);\nconst last = botMsgs[botMsgs.length - 1];\nconst answer = last ? String(last.text || '') : '';\n\n// Tool-call trace: one agent_trace row per turn, written by the Darwin\n// workflow's Save Trace node. Aggregated across ALL turns of the thread.\n// NOTE: the Supabase getAll node can return duplicate rows for this table\n// (observed 4 rows x11 = 44 items, run 214306) — dedupe by row id.\nconst seen = new Set();\nconst traceRows = $('Fetch Trace').all().map(i => i.json)\n  .filter(r => { if (r.id == null || seen.has(r.id)) return false; seen.add(r.id); return true; })\n  .sort((a, b) => a.id - b.id);\n\nconst norm = s => String(s).toLowerCase().replace(/[^a-z0-9]+/g, '_').replace(/^_+|_+$/g, '');\n\nconst metaOf = (row, contentKey) => {\n  if (row.metadata && typeof row.metadata === 'object') return row.metadata;\n  const m = {};\n  for (const k of Object.keys(row)) {\n    if (k === contentKey || k === 'metadata' || k === 'score') continue;\n    const v = row[k];\n    if (v == null || typeof v === 'string' || typeof v === 'number' || typeof v === 'boolean') m[k] = v;\n  }\n  return m;\n};\n\nconst unwrapRow = (row) => {\n  if (!row || typeof row !== 'object') return null;\n  if (row.document && typeof row.document.pageContent === 'string') {\n    return { content: row.document.pageContent, metadata: row.document.metadata || {}, score: row.score ?? null };\n  }\n  if (typeof row.pageContent === 'string') return { content: row.pageContent, metadata: metaOf(row, 'pageContent'), score: row.score ?? null };\n  if (typeof row.content === 'string') return { content: row.content, metadata: metaOf(row, 'content'), score: row.score ?? null };\n  if (typeof row.session_summary === 'string' || typeof row.profile_summary === 'string') {\n    return { content: row.session_summary || row.profile_summary || '', metadata: metaOf(row, 'session_summary'), score: null };\n  }\n  return null;\n};\n\nconst compressObs = (obs) => {\n  if (obs == null) return [];\n  let parsed;\n  try { parsed = JSON.parse(String(obs)); } catch (e) { return [{ content: String(obs).slice(0, 1200), metadata: {}, score: null }]; }\n  // Phase C: unified `search` tool returns { results: [...], meta: {...} } —\n  // each result already carries normalized fields (source, id, group_key, quote,\n  // attribution, deep_link, timestamp, ...). Emit them as rows with the full\n  // result as metadata so scoring can read per-hit source and fields.\n  // toolWorkflow wraps the sub-workflow's items: [{ results: [...], meta: {...} }]\n  const env = Array.isArray(parsed) ? parsed[0] : parsed;\n  if (env && Array.isArray(env.results)) {\n    return env.results.slice(0, 15).map(r => ({\n      content: String((r && (r.snippet || r.quote)) || '').slice(0, 1200),\n      metadata: (r && typeof r === 'object') ? r : {},\n      score: (r && typeof r.score === 'number') ? r.score : null\n    }));\n  }\n  const outer = Array.isArray(parsed) ? parsed : [parsed];\n  const rows = [];\n  for (const item of outer) {\n    const blocks = item && Array.isArray(item.response) ? item.response : [item];\n    for (const b of blocks) {\n      let candidate = b;\n      if (b && b.type === 'text' && typeof b.text === 'string') {\n        try { candidate = JSON.parse(b.text); } catch (e) { candidate = { content: b.text }; }\n      }\n      const row = unwrapRow(candidate);\n      if (row) rows.push(row);\n      if (rows.length >= 10) break;\n    }\n    if (rows.length >= 10) break;\n  }\n  return rows.map(r => ({ content: r.content.slice(0, 1200), metadata: r.metadata, score: r.score }));\n};\n\nconst calledN = [];\nconst steps = [];\ntraceRows.forEach((row, turnIdx) => {\n  let rowSteps = row.steps;\n  if (typeof rowSteps === 'string') { try { rowSteps = JSON.parse(rowSteps); } catch (e) { rowSteps = []; } }\n  if (!Array.isArray(rowSteps)) rowSteps = [];\n  for (const st of rowSteps) {\n    if (!st || !st.tool) continue;\n    const n = norm(st.tool);\n    if (!calledN.includes(n)) calledN.push(n);\n    steps.push({ tool: n, turn: turnIdx + 1, input: st.input ?? null, rows: compressObs(st.observation) });\n  }\n});\n\nconst baseUrl = String($vars.instance_url || 'https://darwin-staging.app.n8n.cloud').replace(/\\/$/, '');\nconst status = turnErrored ? 'error' : (answer ? 'pending' : 'error');\nreturn [{ json: {\n  run_id: runId,\n  case_id: caseId,\n  case_slug: c.slug,\n  status: status,\n  agent_output: answer.slice(0, 8000),\n  called_tools: calledN,\n  raw_steps: steps.slice(0, 60),\n  session_id: threadTs,\n  execution_url: baseUrl + '/workflow/' + $workflow.id + '/executions/' + $execution.id\n} }];"
      },
      "id": "b347681f-0cc1-4818-85cb-26165288dcbd",
      "name": "Capture",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        480,
        608
      ]
    },
    {
      "parameters": {
        "jsCode": "const r = $input.first().json;\nconst c = $('Load Case').item.json;\nif (r.status === 'error') return [{ json: Object.assign({}, r, { metrics: { reason: 'agent run errored or no bot reply in thread — see execution link' } }) }];\n\nconst norm = s => String(s).toLowerCase().replace(/[^a-z0-9]+/g, '_').replace(/^_+|_+$/g, '');\nconst normText = s => String(s).toLowerCase().replace(/[^a-z0-9\\s]/g, ' ').replace(/\\s+/g, ' ').trim();\nconst TOOL_SOURCE = { session_search: 'sessions', search: 'sessions', get_session: 'sessions', get_sessions: 'sessions', slack_store: 'slack', forum_store: 'forum', plain: 'plain', gong: 'gong', get_jobs: 'jobs', get_opportunities: 'opportunities' };\n\nconst hits = [];\nfor (const s of (r.raw_steps || [])) {\n  if (!s.tool || !(s.tool in TOOL_SOURCE)) continue;\n  const source = TOOL_SOURCE[s.tool];\n  for (const row of (s.rows || [])) {\n    const m = row.metadata || {};\n    const str = v => (typeof v === 'string' ? v : null);\n    const num = v => (typeof v === 'number' ? v : null);\n    const content = String(row.content || '');\n    // Phase C: unified `search` rows carry normalized fields + per-hit source.\n    const rowSource = str(m.source) || source;\n    const h = {\n      rank: hits.length + 1, source: rowSource,\n      snippet: content.length > 300 ? content.slice(0, 300) + '...' : content,\n      quote: str(m.quote), attribution: str(m.attribution), project_name: str(m.project_name),\n      timestamp: num(m.timestamp), title: str(m.title), deep_link: str(m.deep_link),\n      created_at: str(m.created_at),\n      score: row.score ?? null, id: str(m.id) ?? String(hits.length + 1), group_key: str(m.group_key)\n    };\n    if (rowSource === 'sessions') { h.id = str(m.session_id) ?? h.id; h.group_key = str(m.session_id) ?? h.group_key; h.timestamp = num(m.start) ?? h.timestamp; h.attribution = str(m.participant_name) ?? h.attribution; h.created_at = str(m.created_at) ?? h.created_at; }\n    else if (rowSource === 'gong') { h.id = str(m.call_id) ?? h.id; h.group_key = str(m.call_id) ?? h.group_key; h.title = h.title ?? str(m.title); h.deep_link = str(m.url) ?? h.deep_link; h.created_at = str(m.date) ?? h.created_at; }\n    else if (rowSource === 'forum') { h.id = str(m.post_id) ?? h.id; h.group_key = h.group_key ?? (m.topic_id ? 'topic ' + m.topic_id : null); h.title = h.title ?? str(m.topic_title); h.deep_link = str(m.post_url) ?? h.deep_link; }\n    else if (rowSource === 'plain') { h.id = str(m.thread_id) ?? h.id; h.group_key = str(m.thread_id) ?? h.group_key; h.title = h.title ?? str(m.thread_title); }\n    else if (rowSource === 'slack') { h.id = str(m.ts) ?? h.id; h.group_key = str(m.ts) ?? h.group_key; h.deep_link = str(m.url) ?? h.deep_link; }\n    else { h.id = str(m.id) ?? h.id; h.group_key = h.group_key ?? str(m.id); h.title = h.title ?? str(m.theme); }\n    hits.push(h);\n  }\n}\n\nlet expected = c.expected || {};\nif (typeof expected === 'string') { try { expected = JSON.parse(expected); } catch (e) { expected = {}; } }\nconst requiredFields = Array.isArray(c.required_fields) ? c.required_fields : [];\nconst expectedTools = (Array.isArray(c.expected_tools) ? c.expected_tools : []).map(norm);\n\nconst hitText = h => normText([h.quote, h.snippet, h.title].filter(Boolean).join(' '));\nconst isSess = (h, id) => h.source === 'sessions' && (h.group_key === id || h.id === id);\n\nconst expSources = expected.sources || [];\nconst expSessions = expected.session_ids || [];\nconst absentIds = expected.absent_session_ids || [];\nconst expQuotes = expected.quotes || [];\n\nconst distinctSources = new Set(hits.map(h => h.source)).size;\nconst sourceRecall = expSources.length ? expSources.filter(s => hits.some(h => h.source === s)).length / expSources.length : null;\nconst sessionRecall = expSessions.length ? expSessions.filter(id => hits.some(h => isSess(h, id))).length / expSessions.length : null;\nconst absentCompliance = absentIds.length ? 1 - absentIds.filter(id => hits.some(h => isSess(h, id))).length / absentIds.length : null;\nconst quoteRecall = expQuotes.length ? expQuotes.filter(q => hits.some(h => hitText(h).includes(normText(q)))).length / expQuotes.length : null;\n\nlet mrr = null;\nif (expSessions.length + expQuotes.length + expSources.length > 0) {\n  mrr = 0;\n  for (const h of hits) {\n    const match = expSessions.some(id => isSess(h, id)) || expQuotes.some(q => hitText(h).includes(normText(q))) || expSources.includes(h.source);\n    if (match) { mrr = 1 / h.rank; break; }\n  }\n}\n\nconst top5 = hits.slice(0, 5);\nconst fieldCompleteness = requiredFields.length && top5.length\n  ? top5.filter(h => requiredFields.every(f => h[f] !== null && h[f] !== undefined && h[f] !== '')).length / top5.length\n  : null;\n\nconst groupDiversity = new Set(hits.map(h => h.group_key).filter(Boolean)).size;\n\nconst called = (r.called_tools || []).map(norm);\nconst routing = expectedTools.length ? expectedTools.some(t => called.includes(t)) : null;\n\nconst answer = normText(r.agent_output || '');\nconst answerQuoteRecall = expQuotes.length ? expQuotes.filter(q => answer.includes(normText(q))).length / expQuotes.length : null;\nconst answerSessionRecall = expSessions.length ? expSessions.filter(id => String(r.agent_output || '').includes(id)).length / expSessions.length : null;\n\nconst layers = [];\nif (routing !== null) layers.push(routing);\nif (answerQuoteRecall !== null && requiredFields.length) layers.push(answerQuoteRecall > 0);\nif (hits.length) {\n  if (expSources.length) layers.push(sourceRecall === 1);\n  if (expSessions.length) layers.push(sessionRecall > 0);\n  if (absentIds.length) layers.push(absentCompliance === 1);\n  if (expected.min_sources) layers.push(distinctSources >= expected.min_sources);\n  if (requiredFields.length) layers.push(fieldCompleteness === 1);\n}\nconst status = layers.length && layers.every(Boolean) ? 'pass' : 'fail';\n\nconst zeroTools = [];\nfor (const s of (r.raw_steps || [])) {\n  if (s.tool && Array.isArray(s.rows) && s.rows.length === 0 && (s.tool in TOOL_SOURCE)) zeroTools.push(s.tool);\n}\nlet reason = '—';\nif (status !== 'pass') {\n  if (routing === false) {\n    reason = 'never called ' + (c.expected_tools || []).join(' / ') + ' (used: ' + ((r.called_tools || []).join(', ') || 'none') + ')';\n  } else if (!(r.called_tools || []).length) {\n    reason = 'agent answered without calling any tool';\n  } else if (!hits.length) {\n    reason = 'no retrieval hits captured' + (zeroTools.length ? ' — ' + zeroTools.join(', ') + ' returned 0 rows' : '');\n  } else if (absentIds.length && absentCompliance !== null && absentCompliance < 1) {\n    const leaked = absentIds.filter(id => hits.some(h => isSess(h, id)));\n    reason = 'unapproved session surfaced (' + leaked.length + ' of ' + absentIds.length + ' guarded)';\n  } else if (expSources.length && sourceRecall !== null && sourceRecall < 1) {\n    const missing = expSources.filter(s => !hits.some(h => h.source === s));\n    const zero = zeroTools.filter(t => missing.includes(TOOL_SOURCE[t]));\n    reason = 'no hits from expected source: ' + missing.join(', ') + (zero.length ? ' — ' + zero.join(', ') + ' returned 0 rows' : '');\n  } else if (expSessions.length && sessionRecall === 0) {\n    reason = 'none of the ' + expSessions.length + ' expected sessions surfaced in ' + hits.length + ' hits';\n  } else if (expected.min_sources && distinctSources < expected.min_sources) {\n    reason = 'too few sources: ' + distinctSources + ' < ' + expected.min_sources + ' required';\n  } else if (requiredFields.length && fieldCompleteness !== null && fieldCompleteness < 1) {\n    const missingFields = requiredFields.filter(f => hits.some(h => h[f] == null || h[f] === ''));\n    reason = 'hits missing required fields: ' + (missingFields.length ? missingFields.join(', ') : requiredFields.join(', '));\n  } else if (answerQuoteRecall === 0 && requiredFields.length) {\n    reason = 'answer missing expected quotes';\n  } else {\n    reason = 'see metrics';\n  }\n}\n\nreturn [{ json: Object.assign({}, r, {\n  status: status,\n  metrics: {\n    reason: reason,\n    routing: routing,\n    answer_quote_recall: answerQuoteRecall,\n    answer_session_recall: answerSessionRecall,\n    retrieval_hits: hits.length,\n    retrieval: {\n      source_recall: sourceRecall,\n      distinct_sources: distinctSources,\n      session_recall: sessionRecall,\n      absent_compliance: absentCompliance,\n      quote_recall: quoteRecall,\n      mrr: mrr,\n      field_completeness: fieldCompleteness,\n      group_diversity: groupDiversity\n    }\n  }\n}) }];"
      },
      "id": "c9d85c35-f188-4955-8236-fc798ddd93f2",
      "name": "Score",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        704,
        608
      ]
    },
    {
      "parameters": {
        "tableId": "retrieval_results",
        "fieldsUi": {
          "fieldValues": [
            {
              "fieldId": "run_id",
              "fieldValue": "={{ $json.run_id }}"
            },
            {
              "fieldId": "case_id",
              "fieldValue": "={{ $json.case_id }}"
            },
            {
              "fieldId": "case_slug",
              "fieldValue": "={{ $json.case_slug }}"
            },
            {
              "fieldId": "mechanism",
              "fieldValue": "darwin-slack"
            },
            {
              "fieldId": "target",
              "fieldValue": "={{ $vars.env }}"
            },
            {
              "fieldId": "status",
              "fieldValue": "={{ $json.status }}"
            },
            {
              "fieldId": "agent_output",
              "fieldValue": "={{ $json.agent_output }}"
            },
            {
              "fieldId": "called_tools",
              "fieldValue": "={{ $json.called_tools }}"
            },
            {
              "fieldId": "raw_steps",
              "fieldValue": "={{ $json.raw_steps }}"
            },
            {
              "fieldId": "metrics",
              "fieldValue": "={{ $json.metrics }}"
            },
            {
              "fieldId": "execution_url",
              "fieldValue": "={{ $json.execution_url }}"
            },
            {
              "fieldId": "session_id",
              "fieldValue": "={{ $json.session_id }}"
            }
          ]
        }
      },
      "id": "673a92b6-0693-4e89-a222-a01532c39cd8",
      "name": "Save Result",
      "type": "n8n-nodes-base.supabase",
      "typeVersion": 1,
      "position": [
        928,
        608
      ],
      "credentials": {
        "supabaseApi": {
          "id": "gjrMKYK5nCbL3TLf",
          "name": "Supabase Tests"
        }
      }
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict",
            "version": 3
          },
          "conditions": [
            {
              "leftValue": "={{ $json.is_first }}",
              "operator": {
                "operation": "true",
                "singleValue": true,
                "type": "boolean"
              },
              "id": "cond-first"
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "id": "0fcd51bc-d32d-4042-b84b-c2700d7f8e46",
      "name": "Is First Turn?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.3,
      "position": [
        256,
        304
      ]
    },
    {
      "parameters": {
        "select": "channel",
        "channelId": {
          "__rl": true,
          "mode": "id",
          "value": "={{ $('Case Input').item.json.channel_id }}"
        },
        "text": "=<@{{ $('Case Input').item.json.bot_id }}> {{ $json.text }}",
        "otherOptions": {
          "includeLinkToWorkflow": false
        }
      },
      "id": "9f5c3156-6deb-445d-8c26-8c3808721e09",
      "name": "Post First",
      "type": "n8n-nodes-base.slack",
      "typeVersion": 2.3,
      "position": [
        480,
        208
      ],
      "webhookId": "289229fb-81d4-4e50-8720-96629bdbd1a1",
      "credentials": {
        "slackApi": {
          "id": "xyfYErKBBCTrIiMl",
          "name": "Robs Slack"
        }
      }
    },
    {
      "parameters": {
        "assignments": {
          "assignments": [
            {
              "id": "msg-ts",
              "name": "msg_ts",
              "value": "={{ $json.message && $json.message.ts ? $json.message.ts : ($json.message_timestamp || $json.ts) }}",
              "type": "string"
            },
            {
              "id": "turn-text",
              "name": "text",
              "value": "={{ $('Build Turns').item.json.text }}",
              "type": "string"
            }
          ]
        },
        "options": {}
      },
      "id": "9e0f61ee-975c-4c0f-b0a2-c4eee3273968",
      "name": "Record Ts",
      "type": "n8n-nodes-base.set",
      "typeVersion": 3.5,
      "position": [
        704,
        304
      ]
    },
    {
      "parameters": {
        "select": "channel",
        "channelId": {
          "__rl": true,
          "mode": "id",
          "value": "={{ $('Case Input').item.json.channel_id }}"
        },
        "text": "=<@{{ $('Case Input').item.json.bot_id }}> {{ $json.text }}",
        "otherOptions": {
          "includeLinkToWorkflow": false,
          "thread_ts": {
            "replyValues": {
              "thread_ts": "={{ $('Post First').first().json.message.ts }}"
            }
          }
        }
      },
      "id": "6bc2d83a-3b0f-489b-827a-3d4fc44751a5",
      "name": "Post Reply",
      "type": "n8n-nodes-base.slack",
      "typeVersion": 2.3,
      "position": [
        480,
        400
      ],
      "webhookId": "07231d3a-a629-4def-a22e-51f7158c879f",
      "credentials": {
        "slackApi": {
          "id": "xyfYErKBBCTrIiMl",
          "name": "Robs Slack"
        }
      }
    },
    {
      "parameters": {
        "options": {}
      },
      "id": "2cbfd3a5-65c3-4380-ab07-50cf7471037f",
      "name": "Turns",
      "type": "n8n-nodes-base.splitInBatches",
      "typeVersion": 3,
      "position": [
        32,
        304
      ]
    },
    {
      "parameters": {
        "amount": 15
      },
      "id": "0d7e870b-69ac-498b-a1c2-d6ba77fb16c7",
      "name": "Pace Reply",
      "type": "n8n-nodes-base.wait",
      "typeVersion": 1.1,
      "position": [
        928,
        304
      ],
      "webhookId": "4f1dd732-a333-4553-9c12-e35fcd9aed9d"
    },
    {
      "parameters": {
        "url": "={{ 'https://slack.com/api/reactions.get?channel=' + $('Case Input').item.json.channel_id + '&timestamp=' + $('Record Ts').item.json.msg_ts }}",
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "slackApi",
        "options": {}
      },
      "id": "657cfe01-0f1f-4182-a784-b5e122b34f8e",
      "name": "Get Reactions",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.5,
      "position": [
        1152,
        304
      ],
      "credentials": {
        "slackApi": {
          "id": "xyfYErKBBCTrIiMl",
          "name": "Robs Slack"
        }
      }
    },
    {
      "parameters": {
        "jsCode": "const reactions = ($json.message && $json.message.reactions) || [];\nconst done = reactions.some(r => r.name === 'white_check_mark' || r.name === 'x');\nconst curTs = $('Record Ts').item.json.msg_ts;\nlet prev = null;\ntry { prev = $('Check Done').item.json; } catch (e) { prev = null; }\nconst polls = (prev && prev.msg_ts === curTs) ? (prev.polls || 0) + 1 : 1;\nreturn [{ json: { done: done, timedOut: !done && polls >= 24, polls: polls, msg_ts: curTs } }];"
      },
      "id": "c35030dc-3f9b-4179-8442-767bb1fa7763",
      "name": "Check Done",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1376,
        304
      ]
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict",
            "version": 3
          },
          "conditions": [
            {
              "leftValue": "={{ $json.done }}",
              "operator": {
                "operation": "true",
                "singleValue": true,
                "type": "boolean"
              },
              "id": "cond-done"
            },
            {
              "leftValue": "={{ $json.timedOut }}",
              "operator": {
                "operation": "true",
                "singleValue": true,
                "type": "boolean"
              },
              "id": "cond-timeout"
            }
          ],
          "combinator": "or"
        },
        "options": {}
      },
      "id": "573874db-26f7-45e8-aa64-a8332b111800",
      "name": "Done?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.3,
      "position": [
        1600,
        304
      ]
    },
    {
      "parameters": {
        "jsCode": "const threadTs = $('Post First').first().json.message.ts;\nlet finalMsgTs = threadTs;\ntry {\n  const lastReply = $('Post Reply').last().json;\n  if (lastReply && lastReply.message && lastReply.message.ts) finalMsgTs = lastReply.message.ts;\n} catch (e) { finalMsgTs = threadTs; }\nreturn [{ json: { thread_ts: threadTs, final_msg_ts: finalMsgTs } }];"
      },
      "id": "98ced5db-ab08-44e0-91d1-323218143c3a",
      "name": "Final Ts",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -192,
        608
      ]
    },
    {
      "parameters": {
        "url": "={{ 'https://slack.com/api/reactions.get?channel=' + $('Case Input').item.json.channel_id + '&timestamp=' + $('Final Ts').first().json.final_msg_ts }}",
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "slackApi",
        "options": {}
      },
      "id": "8e15cedf-fb3a-4824-8fb7-bb8e43ce9218",
      "name": "Final Reactions",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.5,
      "position": [
        480,
        720
      ],
      "credentials": {
        "slackApi": {
          "id": "xyfYErKBBCTrIiMl",
          "name": "Robs Slack"
        }
      }
    }
  ],
  "connections": {
    "Case Input": {
      "main": [
        [
          {
            "node": "Load Case",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Load Case": {
      "main": [
        [
          {
            "node": "Build Turns",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build Turns": {
      "main": [
        [
          {
            "node": "Turns",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Capture": {
      "main": [
        [
          {
            "node": "Score",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Score": {
      "main": [
        [
          {
            "node": "Save Result",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Is First Turn?": {
      "main": [
        [
          {
            "node": "Post First",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Post Reply",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Post First": {
      "main": [
        [
          {
            "node": "Record Ts",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Record Ts": {
      "main": [
        [
          {
            "node": "Pace Reply",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Post Reply": {
      "main": [
        [
          {
            "node": "Record Ts",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Turns": {
      "main": [
        [
          {
            "node": "Final Ts",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Is First Turn?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Pace Reply": {
      "main": [
        [
          {
            "node": "Get Reactions",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Get Reactions": {
      "main": [
        [
          {
            "node": "Check Done",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Check Done": {
      "main": [
        [
          {
            "node": "Done?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Done?": {
      "main": [
        [
          {
            "node": "Turns",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Pace Reply",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Fetch Thread": {
      "main": [
        [
          {
            "node": "Final Reactions",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Final Reactions": {
      "main": [
        [
          {
            "node": "Fetch Trace",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Final Ts": {
      "main": [
        [
          {
            "node": "Fetch Thread",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Fetch Trace": {
      "main": [
        [
          {
            "node": "Capture",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1",
    "availableInMCP": true,
    "binaryMode": "separate"
  },
  "triggerCount": 0,
  "versionId": "53eb8997-f7f9-4428-84cb-ea1b04c0de65",
  "owner": {
    "type": "team",
    "teamId": "2uAQ2XYnaFVSte2z",
    "teamName": "Darwin"
  },
  "parentFolderId": "VOBjsASxrcPKChe6",
  "isArchived": false,
  "nodeGroups": []
}