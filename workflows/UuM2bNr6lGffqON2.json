{
  "id": "UuM2bNr6lGffqON2",
  "name": "IntegrationDispatch",
  "nodes": [
    {
      "parameters": {
        "workflowInputs": {
          "values": [
            {
              "name": "run_id"
            },
            {
              "name": "case_id"
            }
          ]
        }
      },
      "id": "1dd42c88-be56-4c03-8279-f0aef77a3ca3",
      "name": "Case Input",
      "type": "n8n-nodes-base.executeWorkflowTrigger",
      "typeVersion": 1.2,
      "position": [
        -288,
        304
      ]
    },
    {
      "parameters": {
        "operation": "getAll",
        "tableId": "integration_cases",
        "returnAll": true,
        "matchType": "allFilters",
        "filters": {
          "conditions": [
            {
              "keyName": "id",
              "condition": "eq",
              "keyValue": "={{ $('Case Input').item.json.case_id }}"
            }
          ]
        }
      },
      "id": "243a334b-922c-4643-a115-bb7ee0245ec5",
      "name": "Load Case",
      "type": "n8n-nodes-base.supabase",
      "typeVersion": 1,
      "position": [
        -64,
        304
      ],
      "credentials": {
        "supabaseApi": {
          "id": "gjrMKYK5nCbL3TLf",
          "name": "Supabase Tests"
        }
      }
    },
    {
      "parameters": {
        "workflowId": {
          "__rl": true,
          "mode": "id",
          "value": "={{ $json.agent === 'builder' ? 'ZMyr5DWlrfXlhhKs' : 'HDMvthliXrAcZrXp' }}"
        },
        "workflowInputs": {
          "mappingMode": "defineBelow",
          "value": {
            "chatInput": "={{ $json.prompt }}",
            "task": "={{ $json.prompt }}",
            "sessionId": "={{ 'integration-' + $('Case Input').item.json.run_id + '-' + $('Case Input').item.json.case_id }}",
            "session_id": "={{ 'integration-' + $('Case Input').item.json.run_id + '-' + $('Case Input').item.json.case_id }}",
            "channel": "integration-test",
            "env": "test",
            "slack_channel": ""
          },
          "matchingColumns": [],
          "schema": []
        },
        "mode": "each",
        "options": {
          "waitForSubWorkflow": true
        }
      },
      "id": "e5fd4c49-1761-465f-8009-a463c6616f5b",
      "name": "Call Agent",
      "type": "n8n-nodes-base.executeWorkflow",
      "typeVersion": 1.3,
      "position": [
        160,
        304
      ],
      "onError": "continueRegularOutput"
    },
    {
      "parameters": {
        "jsCode": "const c = $('Load Case').item.json;\nconst runId = $('Case Input').item.json.run_id;\nconst r = $input.first().json;\nlet status = 'pass';\nlet answer = '';\nlet called = [];\n\nif (r && r.error) {\n  status = 'error';\n  answer = typeof r.error === 'string' ? r.error : JSON.stringify(r.error);\n} else {\n  if (typeof r.output === 'string') {\n    answer = r.output;\n  } else if (r.output !== undefined) {\n    answer = JSON.stringify(r.output);\n  } else {\n    answer = JSON.stringify(r);\n  }\n  if (Array.isArray(r.tools_used)) {\n    called = r.tools_used;\n  } else if (Array.isArray(r.intermediateSteps)) {\n    for (const s of r.intermediateSteps) {\n      const t = s && s.action && s.action.tool;\n      if (t) called.push(t);\n    }\n  }\n  if (!answer) status = 'error';\n}\n\nconst norm = function (s) { return String(s).toLowerCase().replace(/[^a-z0-9]+/g, '_').replace(/^_+|_+$/g, ''); };\nconst calledN = [];\nfor (const t of called) { const n = norm(t); if (!calledN.includes(n)) calledN.push(n); }\n\n// Full intermediate steps (action + observation) so we can judge tool OUTCOMES,\n// not just names. n8n records a failed tool call as an observation of the shape\n// [{\"error\": \"...\"}] (or {\"error\": \"...\"}); successful calls carry the real output.\nconst steps = Array.isArray(r.intermediateSteps) ? r.intermediateSteps : [];\n\nconst isErrorObs = function (obs) {\n  if (obs == null) return true;\n  const s = String(obs).trim();\n  if (s === '') return true;\n  let parsed;\n  try { parsed = JSON.parse(s); } catch (e) { return false; }\n  const arr = Array.isArray(parsed) ? parsed : [parsed];\n  return arr.length > 0 && arr.every(x => x && typeof x === 'object' && 'error' in x);\n};\n\n// 'ok' | 'failed' | 'not_called' — ok if ANY call to the tool returned real output.\nconst toolOutcome = function (toolRaw) {\n  const n = norm(toolRaw);\n  const hits = steps.filter(s => s && s.action && norm(s.action.tool) === n);\n  if (!hits.length) return 'not_called';\n  return hits.every(s => isErrorObs(s.observation)) ? 'failed' : 'ok';\n};\n\nlet assertions = [];\ntry {\n  assertions = typeof c.assertions === 'string' ? JSON.parse(c.assertions) : (c.assertions || []);\n} catch (e) {\n  status = 'error';\n}\n\nconst answerN = answer.toLowerCase();\nconst results = [];\nif (status !== 'error') {\n  for (const a of assertions) {\n    let pass = false;\n    if (a.type === 'tool_called') pass = calledN.includes(norm(a.value));\n    else if (a.type === 'tool_not_called') pass = !calledN.includes(norm(a.value));\n    else if (a.type === 'contains') pass = answerN.includes(String(a.value).toLowerCase());\n    else if (a.type === 'not_contains') pass = !answerN.includes(String(a.value).toLowerCase());\n    else if (a.type === 'tool_succeeded') pass = toolOutcome(a.value) === 'ok';\n    else if (a.type === 'tool_failed') pass = toolOutcome(a.value) === 'failed';\n    if (!pass) status = 'fail';\n    results.push({ type: a.type, value: a.value, pass: pass });\n  }\n}\n\nreturn [{ json: {\n  run_id: runId,\n  case_id: c.id,\n  slug: c.slug,\n  status: status,\n  called_tools: calledN,\n  output: answer.slice(0, 4000),\n  assertion_results: results,\n  session_id: 'integration-' + runId + '-' + c.id\n} }];"
      },
      "id": "5f2705b3-a3ef-4a72-b1d1-f403f1256efc",
      "name": "Evaluate",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        400,
        304
      ]
    },
    {
      "parameters": {
        "tableId": "integration_results",
        "fieldsUi": {
          "fieldValues": [
            {
              "fieldId": "run_id",
              "fieldValue": "={{ $json.run_id }}"
            },
            {
              "fieldId": "case_id",
              "fieldValue": "={{ $json.case_id }}"
            },
            {
              "fieldId": "instance",
              "fieldValue": "={{ $vars.env }}"
            },
            {
              "fieldId": "status",
              "fieldValue": "={{ $json.status }}"
            },
            {
              "fieldId": "called_tools",
              "fieldValue": "={{ JSON.stringify($json.called_tools) }}"
            },
            {
              "fieldId": "assertion_results",
              "fieldValue": "={{ JSON.stringify($json.assertion_results) }}"
            },
            {
              "fieldId": "output",
              "fieldValue": "={{ $json.output }}"
            },
            {
              "fieldId": "session_id",
              "fieldValue": "={{ $json.session_id }}"
            }
          ]
        }
      },
      "id": "483f0ea8-ae4d-42e7-88b3-03a134ed18c1",
      "name": "Save Result",
      "type": "n8n-nodes-base.supabase",
      "typeVersion": 1,
      "position": [
        640,
        304
      ],
      "credentials": {
        "supabaseApi": {
          "id": "gjrMKYK5nCbL3TLf",
          "name": "Supabase Tests"
        }
      }
    }
  ],
  "connections": {
    "Case Input": {
      "main": [
        [
          {
            "node": "Load Case",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Load Case": {
      "main": [
        [
          {
            "node": "Call Agent",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Call Agent": {
      "main": [
        [
          {
            "node": "Evaluate",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Evaluate": {
      "main": [
        [
          {
            "node": "Save Result",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1",
    "availableInMCP": true,
    "binaryMode": "separate",
    "errorWorkflow": "iDcm070UekOYt0f1"
  },
  "triggerCount": 0,
  "versionId": "15fb77d2-f3ff-440d-a353-a2f7fa6948de",
  "owner": {
    "type": "team",
    "teamId": "2uAQ2XYnaFVSte2z",
    "teamName": "Darwin"
  },
  "parentFolderId": "CVkK9Ao2Onp9a2Gp",
  "isArchived": false,
  "nodeGroups": []
}